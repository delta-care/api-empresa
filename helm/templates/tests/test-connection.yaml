apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-empresa.fullname" . }}-test-connection"
  labels:
    {{- include "api-empresa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-empresa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
