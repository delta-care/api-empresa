# app.profile, k8s.namespace, image.tag e ops.env definidos via jenkins
app:
  name: api-empresa
  profile:
k8s:
  namespace:
image:
  repository: deltacare/api-empresa 
  tag:
service:
  type: ClusterIP
  port: 8080
ops:
  env:
